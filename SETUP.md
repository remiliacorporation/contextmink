# contextmink Setup

The full setup guide is in [docs/setup.md](docs/setup.md). It covers vendoring
`contextmink` into another repository, adding the `scripts/contextmink`
launcher, configuring `.contextmink.toml`, and installing the agent instruction
block.
