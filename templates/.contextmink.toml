profile = "replace-with-workspace-name"

# Built-in policy already excludes .git, target, node_modules, and .venv trees.
# Add only project-specific high-output paths such as generated assets, exports,
# caches, large data trees, or vendored reference corpora.
exclude_globs = []

# Optional spawn safety for repository-owned critical paths:
# destructive_guard_recursive_delete_fragments = ["protected_cache"]
# destructive_guard_delete_fragments = ["critical.sqlite"]
